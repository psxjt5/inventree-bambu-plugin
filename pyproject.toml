#This file is used to package the InvenTreeBambuLabMachines plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.14.0
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-bambu-plugin"
description = "Machine drivers and support for Bambu Lab 3D printers."
dynamic = ["version"]
authors = [
    { name = "James Todd",email = "James.Todd@nottingham.ac.uk" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/psxjt5/inventree-bambu-plugin"

[project.entry-points."inventree_plugins"]
InvenTreeBambuPlugin = "inventree_bambu.core:InvenTreeBambuPlugin"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "inventree_bambu.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["inventree_bambu"]
